python-dotenv

# Frontend
# HF Spaces ya instala gradio[oauth]==4.44.1 — no lo pineamos aquí
# para evitar conflictos; huggingface_hub<0.27.0 evita ImportError de HfFolder
gradio
huggingface_hub<0.27.0

# Motor IA Primario (Gemini)
# google-generativeai usa gRPC/httpx — sin conflicto de websockets con gradio
google-generativeai

# Fallback IA (OpenAI + DuckDuckGo)
openai
ddgs

# Utilidades
pandas
httpx
