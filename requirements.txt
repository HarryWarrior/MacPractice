# ── Backend + IA ──────────────────────────────────────────
python-dotenv>=1.0.0

# Frontend
gradio

# Motor IA Primario (Gemini — nuevo SDK oficial)
google-genai>=1.0.0

# Fallback IA (OpenAI + DuckDuckGo)
openai>=1.0.0
ddgs
# Utilidades
pandas>=2.0.0
httpx>=0.25.0
