python-dotenv

# Frontend
# Versiones exactas para evitar bug "argument of type 'bool' is not iterable"
# en gradio_client/utils.py que rompe el endpoint /info en HF Spaces
gradio==4.44.1
gradio-client==0.9.1
huggingface_hub<0.27.0

# Motor IA Primario (Gemini)
# google-generativeai usa gRPC/httpx — sin conflicto de websockets con gradio
google-generativeai

# Fallback IA (OpenAI + DuckDuckGo)
openai
ddgs

# Utilidades
pandas
httpx
